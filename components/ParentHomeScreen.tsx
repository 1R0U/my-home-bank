import { Text, View } from "react-native";

export default function ParentHomeScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-white">
      <Text className="text-2xl font-bold">大人用画面（仮）</Text>
    </View>
  );
}
