Retry PR: include bugfixes and tests.

Changes:
- `app/bank.tsx`: make scrollable, use mock data, reuse account lookup. NOTE: currently uses `MOCK_CURRENT_USER` directly (mock user). Replace with authenticated user (Supabase) when integrating auth.
- `app/index.tsx`: add bank link.
- `babel.config.js`: fix closing braces. (Note: moving `nativewind/babel` to presets is not present in this branch; only a whitespace change after `api.cache(true);` was added.)
- `tests/navigation.test.mjs`: add simple nav tests (file-content checks).

Tests: the new tests use `fs.readFileSync` to verify `href="/bank"` and `href="/"` exist in source; they are lightweight checks. For full render/navigation tests consider adding `@testing-library/react-native` in a follow-up.
